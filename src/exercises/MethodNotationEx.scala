package exercises

object MethodNotationEx extends App{

  // in lectures

  // 1. Overload + opperator
  //      leo +  "the rockstar" => new Person "Leo (the rockstar) and fav movie"

  // 2. add en age to the person class
        // add unary + operator =>new peroins with  increments age + 1

  // 3. Add a "learns" methon in the person class ,receives string par and returns
  // retuens sentencs "__ learns scala"
  //  add learns scala method; with no params and call the learns method with 'scala' as a param
  // Use it on post fic notation

  //4. overload apply method to recieve an int and return a string
  // leo.apply(2) => "Leo watched  fav movie 2 times"
}
